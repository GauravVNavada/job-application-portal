{
    "name": "job-portal-monorepo",
    "version": "1.0.0",
    "workspaces": [
        "frontend",
        "backend"
    ],
    "scripts": {
        "postinstall": "npm rebuild prisma && npm rebuild lightningcss && npm rebuild @tailwindcss/oxide && npx prisma generate --schema=backend/prisma/schema.prisma || true"
    },
    "dependencies": {},
    "devDependencies": {
        "prisma": "5.10.2"
    }
}