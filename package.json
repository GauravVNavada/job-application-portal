{
    "name": "job-portal-monorepo",
    "version": "1.0.0",
    "scripts": {
        "postinstall": "cd backend && npm install && npx prisma generate"
    },
    "dependencies": {},
    "devDependencies": {}
}